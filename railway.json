{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksVersion": "1.38.0",  // Use a known stable version
    "buildEnvironment": "V2"       // Use stable build environment
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  },
  "variables": {
    "NODE_ENV": "production",
    "PORT": "${{PORT}}"
  }
} 
//ist that clear/