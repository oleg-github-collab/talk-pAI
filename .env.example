# Talk pAI Environment Variables - Railway.app Configuration
# Copy this file to .env and configure for your deployment

# Application Settings
NODE_ENV=production
PORT=8080
LOG_LEVEL=info

# Database - Railway PostgreSQL (automatically provided)
# DATABASE_URL is automatically set by Railway when you add PostgreSQL
# DATABASE_URL=postgresql://username:password@host:port/database

# AI Services - Required for Aiden AI Companion
OPENAI_API_KEY=your-openai-api-key-here

# Authentication & Security
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# Optional: Advanced Features
# REDIS_URL=redis://localhost:6379  # For caching (if you add Redis)

# Optional: Email Notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-email-password
# SMTP_FROM="Talk pAI <noreply@yourapp.com>"

# Optional: File Upload Settings
UPLOAD_MAX_SIZE=50mb

# Optional: Rate Limiting
RATE_LIMIT_WINDOW=900000  # 15 minutes
RATE_LIMIT_MAX=100        # 100 requests per window

# Optional: Feature Flags
ENABLE_REGISTRATION=true
ENABLE_AI_FEATURES=true

# Optional: Admin Settings
ADMIN_EMAIL=admin@yourapp.com

# Railway automatically provides:
# - DATABASE_URL (when PostgreSQL is added)
# - RAILWAY_ENVIRONMENT
# - PORT (automatically set to 8080)

# Notes for Railway deployment:
# 1. Add PostgreSQL service in Railway dashboard
# 2. Set OPENAI_API_KEY in Railway environment variables
# 3. Set JWT_SECRET to a secure random string
# 4. Other variables are optional but recommended for production